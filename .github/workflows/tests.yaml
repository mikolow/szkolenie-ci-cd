name: tests
on: [ push ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up node latest
        uses: actions/setup-node@v4
        with:
          node-version: 'latest'
      - run: npm ci
      - run: npm test

